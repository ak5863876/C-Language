#include <stdio.h>

int main(){
    // Printing the current age
    int age = 25;
    printf("Age: %d",age); 
    // Creating a new age variable
    age = 31;
    printf("\nNew Age: %d",age); //Declaring new age on new line
    return 0;
}